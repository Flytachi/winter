{
    "name": "flytachi/winter",
    "description": "Winter Framework (CLI/WEB)",
    "keywords": ["Winter", "winter", "framework"],
    "type": "project",
    "scripts": {
        "post-create-project-cmd": [
            "@php call storage init",
            "chmod -R 777 storage",
            "@php call cfg init"
        ]
    },
    "autoload": {
        "psr-4": {
            "Main\\": "main/"
        }
    },
    "require": {
        "php": ">=8.4",
        "flytachi/winter-kernel": "^4.0"
    },
    "require-dev": {
        "monolog/monolog": "@stable"
    }
}
